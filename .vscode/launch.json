{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Decision Tree Builder (Module)",
            "type": "debugpy",
            "request": "launch",
            "module": "agent_framework_demo.decision_tree_builder",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}",
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}